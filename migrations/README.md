# Migrations

This directory is required by sequelize-cli. Add migration files here, e.g.:

- 20240101000000-create-users.js

If there are no migrations yet, this folder can remain empty. Running `npx sequelize-cli db:migrate` will then report that there are no migrations to run.
